'use client'
// import dynamic from "next/dynamic";

// Dynamically import QuillEditor to prevent SSR issues
// const TextEditor = dynamic(() => import("@/components/TextEditor/TextEditor"), {
//   ssr: false,
// });

export default function page() {
  return (
    <div>
      {/* <TextEditor /> */}
    </div>
  );
}
